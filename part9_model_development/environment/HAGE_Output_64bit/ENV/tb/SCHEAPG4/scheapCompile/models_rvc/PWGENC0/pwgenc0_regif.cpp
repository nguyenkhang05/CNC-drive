// -----------------------------------------------------------------------------
// $Id: pwgenc0_regif.cpp,v 1.3 2020/01/15 13:23:55 landang Exp $
//
// Copyright(c) 2018-2020 Renesas Electronics Corporation
// Copyright(c) 2018-2020 Renesas Design Vietnam Co., Ltd.
// RENESAS ELECTRONICS CONFIDENTIAL AND PROPRIETARY.
// This program must be used solely for the purpose for which
// it was furnished by Renesas Electronics Corporation. No part of this
// program may be reproduced or disclosed to others, in any
// form, without the prior written permission of Renesas Electronics
// Corporation.
// -----------------------------------------------------------------------------
// This file is generated by Register I/F generator
//    gen_regif.py 1.9 2013/12/04 09:51:36 sontran
//    gen_regif_class.py 1.60 2014/10/24 09:42:15 ducduong
//    regif_cpp.skl 1.53 2014/10/13 03:19:16 sontran
//
// Input file : input/pwgenc0_regif.txt
////////////////////////////////////////////////////////////////////////////////
// %MODULE pwgenc0
//     #              name     offset_size
//     %%REG_INSTANCE reg_def  6 
// 
// %REG_CHANNEL reg_def                                                       
//     %%TITLE    name        reg_name      wsize   rsize      length offset   access   init   support  callback
//     %%REG      PWG0CSDR    PWG0CSDR        16    8|16|32    32     0x00     R|W      0x0     TRUE     -
//     %%REG      PWG0CRDR    PWG0CRDR           32 8|16|32    32     0x04     R|W      0x0     TRUE     -
//     %%REG      PWG0CTDR    PWG0CTDR        16    8|16|32    32     0x08     R|W      0x0     TRUE     -
//     %%REG      PWG0RDT     PWG0RDT       8       8|16|32    32     0x0C     R|W      0x0     TRUE     -
//     %%REG      PWG0ST      PWG0ST          16    8|16|32    32     0x10     R|W      0x0     TRUE     -
//     %%REG      PWG0CNT     PWG0CNT         16    8|16|32    32     0x14     R|W      0xFFFF  TRUE     -
//     %%REG      PWG0TCR     PWG0TCR       8       8|16|32    32     0x18     R|W      0x1     TRUE     -
//     %%REG      PWG0TCBR    PWG0TCBR      8       8|16|32    32     0x1C     R|W      0x1     TRUE     -
//     %%REG      PWG0CTL     PWG0CTL         16    8|16|32    32     0x20     R|W      0x8000  TRUE     -
//     %%REG      PWG0CSBR    PWG0CSBR        16    8|16|32    32     0x24     R|W      0x0     TRUE     -
//     %%REG      PWG0CRBR    PWG0CRBR           32 8|16|32    32     0x28     R|W      0x0     TRUE     -
//     %%REG      PWG0CTBR    PWG0CTBR        16    8|16|32    32     0x2C     R|W      0x0     TRUE     -
//     %%REG      PWG0TE      PWG0TE        8       8|16|32    32     0x30     R        0x0     TRUE     -
//     %%REG      PWG0TS      PWG0TS        8       8|16|32    32     0x34     R|W      0x0     TRUE     -
//     %%REG      PWG0TT      PWG0TT        8       8|16|32    32     0x38     R|W      0x0     TRUE     -
//     %%REG      PWG0FOT     PWG0FOT       8       8|16|32    32     0x3C     R|W      0x0     TRUE     -
// 
// %REG_NAME PWG0CSDR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CSDR               15      0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0CRDR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CRDR               16      0       0       R|W     TRUE        -
// 
// %REG_NAME PWG0CTDR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CTDR               15      0       0       R|W     TRUE        -
// 
// %REG_NAME PWG0RDT
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    RDTT               1       1       0       R|W     TRUE        -
//     %%BIT    RDT                0       0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0ST
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    FOF                8       8       0       R|W     TRUE        -
//     %%BIT    RSFT               1       1       0       R|W     TRUE        -
//     %%BIT    RSF                0       0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0CNT
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CNT               15      0       0xFFFF   R|W     TRUE        W
// 
// %REG_NAME PWG0TCR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TOE                0       0       1       R|W     TRUE        W
// 
// %REG_NAME PWG0TCBR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TOBE               0       0       1       R|W     TRUE        W
// 
// %REG_NAME PWG0CTL
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TFIE               15      15      1       R|W     TRUE        -
//     %%BIT    TRIE               14      14      0       R|W     TRUE        -
//     %%BIT    FOS                8       8       0       R|W     TRUE        -
//     %%BIT    OCL                7       7       0       R|W     TRUE        -
//     %%BIT    TCUT               5       4       0       R|W     TRUE        -
//     %%BIT    CKS                1       0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0CSBR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CSBR               15      0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0CRBR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CRBR               16      0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0CTBR
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    CTBR               15      0       0       R|W     TRUE        W
// 
// %REG_NAME PWG0TE
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TE                 0       0       0       R       TRUE        -
// 
// %REG_NAME PWG0TS
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TS                 0       0       0       W       TRUE        W
// 
// %REG_NAME PWG0TT
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    TT                 0       0       0       W       TRUE        W
// 
// %REG_NAME PWG0FOT
//     %%TITLE  name               upper   lower   init    access  support     callback
//     %%BIT    FOT                0       0       0       W       TRUE        W
// 
////////////////////////////////////////////////////////////////////////////////
/// @file pwgenc0_regif.cpp
/// @brief Register IF class of model PWGENC0
/// $Id: pwgenc0_regif.cpp,v 1.3 2020/01/15 13:23:55 landang Exp $
/// $Date: 2020/01/15 13:23:55 $
/// $Revison$
/// $Author: landang $
////////////////////////////////////////////////////////////////////////////////
#include "pwgenc0_regif.h"
#ifndef re_printf
#define re_printf get_fileline(__FILE__, __LINE__); _re_printf
#endif//re_printf

/// Constructor of Register IF class: define registers and bits
/// @return none
Cpwgenc0_regif::Cpwgenc0_regif(std::string name, uint buswidth)
    :vpcl::reg_super()
    #ifdef USR_CWR_SYSTEMC
    , cwmem("register", scml_memsize(0x40/4))
    #endif
{
    CommandInit();
    PWG0CSDR = new vpcl::re_register(0x0000, this, "PWG0CSDR", name.c_str());
    PWG0CRDR = new vpcl::re_register(0x0004, this, "PWG0CRDR", name.c_str());
    PWG0CTDR = new vpcl::re_register(0x0008, this, "PWG0CTDR", name.c_str());
    PWG0RDT  = new vpcl::re_register(0x000C, this, "PWG0RDT" , name.c_str());
    PWG0ST   = new vpcl::re_register(0x0010, this, "PWG0ST"  , name.c_str());
    PWG0CNT  = new vpcl::re_register(0x0014, this, "PWG0CNT" , name.c_str());
    PWG0TCR  = new vpcl::re_register(0x0018, this, "PWG0TCR" , name.c_str());
    PWG0TCBR = new vpcl::re_register(0x001C, this, "PWG0TCBR", name.c_str());
    PWG0CTL  = new vpcl::re_register(0x0020, this, "PWG0CTL" , name.c_str());
    PWG0CSBR = new vpcl::re_register(0x0024, this, "PWG0CSBR", name.c_str());
    PWG0CRBR = new vpcl::re_register(0x0028, this, "PWG0CRBR", name.c_str());
    PWG0CTBR = new vpcl::re_register(0x002C, this, "PWG0CTBR", name.c_str());
    PWG0TE   = new vpcl::re_register(0x0030, this, "PWG0TE"  , name.c_str());
    PWG0TS   = new vpcl::re_register(0x0034, this, "PWG0TS"  , name.c_str());
    PWG0TT   = new vpcl::re_register(0x0038, this, "PWG0TT"  , name.c_str());
    PWG0FOT  = new vpcl::re_register(0x003C, this, "PWG0FOT" , name.c_str());

    // Construct the register pointer list
    mCurReg = NULL;

    uint index = 0;
    mRegMap = new uint [1<<6];
    for (uint i = 0; i < (1<<6); i++) {
        mRegMap[i] = (1<<6);
    }

    mRegMap[0x0000]     = index;
    mRegMap[0x0000 + 1] = index;
    mRegMap[0x0000 + 2] = index;
    mRegMap[0x0000 + 3] = index++;
    mRegMap[0x0004]     = index;
    mRegMap[0x0004 + 1] = index;
    mRegMap[0x0004 + 2] = index;
    mRegMap[0x0004 + 3] = index++;
    mRegMap[0x0008]     = index;
    mRegMap[0x0008 + 1] = index;
    mRegMap[0x0008 + 2] = index;
    mRegMap[0x0008 + 3] = index++;
    mRegMap[0x000C]     = index;
    mRegMap[0x000C + 1] = index;
    mRegMap[0x000C + 2] = index;
    mRegMap[0x000C + 3] = index++;
    mRegMap[0x0010]     = index;
    mRegMap[0x0010 + 1] = index;
    mRegMap[0x0010 + 2] = index;
    mRegMap[0x0010 + 3] = index++;
    mRegMap[0x0014]     = index;
    mRegMap[0x0014 + 1] = index;
    mRegMap[0x0014 + 2] = index;
    mRegMap[0x0014 + 3] = index++;
    mRegMap[0x0018]     = index;
    mRegMap[0x0018 + 1] = index;
    mRegMap[0x0018 + 2] = index;
    mRegMap[0x0018 + 3] = index++;
    mRegMap[0x001C]     = index;
    mRegMap[0x001C + 1] = index;
    mRegMap[0x001C + 2] = index;
    mRegMap[0x001C + 3] = index++;
    mRegMap[0x0020]     = index;
    mRegMap[0x0020 + 1] = index;
    mRegMap[0x0020 + 2] = index;
    mRegMap[0x0020 + 3] = index++;
    mRegMap[0x0024]     = index;
    mRegMap[0x0024 + 1] = index;
    mRegMap[0x0024 + 2] = index;
    mRegMap[0x0024 + 3] = index++;
    mRegMap[0x0028]     = index;
    mRegMap[0x0028 + 1] = index;
    mRegMap[0x0028 + 2] = index;
    mRegMap[0x0028 + 3] = index++;
    mRegMap[0x002C]     = index;
    mRegMap[0x002C + 1] = index;
    mRegMap[0x002C + 2] = index;
    mRegMap[0x002C + 3] = index++;
    mRegMap[0x0030]     = index;
    mRegMap[0x0030 + 1] = index;
    mRegMap[0x0030 + 2] = index;
    mRegMap[0x0030 + 3] = index++;
    mRegMap[0x0034]     = index;
    mRegMap[0x0034 + 1] = index;
    mRegMap[0x0034 + 2] = index;
    mRegMap[0x0034 + 3] = index++;
    mRegMap[0x0038]     = index;
    mRegMap[0x0038 + 1] = index;
    mRegMap[0x0038 + 2] = index;
    mRegMap[0x0038 + 3] = index++;
    mRegMap[0x003C]     = index;
    mRegMap[0x003C + 1] = index;
    mRegMap[0x003C + 2] = index;
    mRegMap[0x003C + 3] = index++;
    mTotalRegNum = index;
    mRegArray = new SRegList* [mTotalRegNum];
    index = 0;
    #ifdef USR_CWR_SYSTEMC
    cw_instantiate_reg(CW_PWG0CSDR  , PWG0CSDR->name(), PWG0CSDR->addr());
    cw_instantiate_reg(CW_PWG0CRDR  , PWG0CRDR->name(), PWG0CRDR->addr());
    cw_instantiate_reg(CW_PWG0CTDR  , PWG0CTDR->name(), PWG0CTDR->addr());
    cw_instantiate_reg(CW_PWG0RDT   , PWG0RDT->name() , PWG0RDT->addr() );
    cw_instantiate_reg(CW_PWG0ST    , PWG0ST->name()  , PWG0ST->addr()  );
    cw_instantiate_reg(CW_PWG0CNT   , PWG0CNT->name() , PWG0CNT->addr() );
    cw_instantiate_reg(CW_PWG0TCR   , PWG0TCR->name() , PWG0TCR->addr() );
    cw_instantiate_reg(CW_PWG0TCBR  , PWG0TCBR->name(), PWG0TCBR->addr());
    cw_instantiate_reg(CW_PWG0CTL   , PWG0CTL->name() , PWG0CTL->addr() );
    cw_instantiate_reg(CW_PWG0CSBR  , PWG0CSBR->name(), PWG0CSBR->addr());
    cw_instantiate_reg(CW_PWG0CRBR  , PWG0CRBR->name(), PWG0CRBR->addr());
    cw_instantiate_reg(CW_PWG0CTBR  , PWG0CTBR->name(), PWG0CTBR->addr());
    cw_instantiate_reg(CW_PWG0TE    , PWG0TE->name()  , PWG0TE->addr()  );
    cw_instantiate_reg(CW_PWG0TS    , PWG0TS->name()  , PWG0TS->addr()  );
    cw_instantiate_reg(CW_PWG0TT    , PWG0TT->name()  , PWG0TT->addr()  );
    cw_instantiate_reg(CW_PWG0FOT   , PWG0FOT->name() , PWG0FOT->addr() );
    #endif
    mCurReg = mRegArray[index++] = new SRegList(PWG0CSDR  , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CRDR  , mCurReg, 0,  4,      "32", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CTDR  , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0RDT   , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0ST    , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CNT   , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0TCR   , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0TCBR  , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CTL   , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CSBR  , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CRBR  , mCurReg, 0,  4,      "32", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0CTBR  , mCurReg, 0,  4,      "16", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0TE    , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0TS    , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0TT    , mCurReg, 0,  4,       "8", "8|16|32");
    mCurReg = mRegArray[index++] = new SRegList(PWG0FOT   , mCurReg, 0,  4,       "8", "8|16|32");

    mRegList = mCurReg;

    (*PWG0CSDR  ) (15,  0, "CSDR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CRDR  ) (16,  0, "CRDR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTDR  ) (15,  0, "CTDR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0RDT   ) ( 1,  1, "RDTT"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0RDT   ) ( 0,  0, "RDT"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0ST    ) ( 8,  8, "FOF"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0ST    ) ( 1,  1, "RSFT"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0ST    ) ( 0,  0, "RSF"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CNT   ) (15,  0, "CNT"     , 0xFFFF    , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0TCR   ) ( 0,  0, "TOE"     , 0x1       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0TCBR  ) ( 0,  0, "TOBE"    , 0x1       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) (15, 15, "TFIE"    , 0x1       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) (14, 14, "TRIE"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) ( 8,  8, "FOS"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) ( 7,  7, "OCL"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) ( 5,  4, "TCUT"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTL   ) ( 1,  0, "CKS"     , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CSBR  ) (15,  0, "CSBR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CRBR  ) (16,  0, "CRBR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0CTBR  ) (15,  0, "CTBR"    , 0x0       , "R|W"   , vpcl::SPP_ENABLE);
    (*PWG0TE    ) ( 0,  0, "TE"      , 0x0       , "R"     , vpcl::SPP_ENABLE);
    (*PWG0TS    ) ( 0,  0, "TS"      , 0x0       , "W"     , vpcl::SPP_ENABLE);
    (*PWG0TT    ) ( 0,  0, "TT"      , 0x0       , "W"     , vpcl::SPP_ENABLE);
    (*PWG0FOT   ) ( 0,  0, "FOT"     , 0x0       , "W"     , vpcl::SPP_ENABLE);

    mFileName = "";
    mInstName = name;
    mLineNum = 0;
    mBusWidth = buswidth;
    mBusByteWidth = mBusWidth/8;   // using in BIG ENDIAN
    mIsReset = false;

    // Constructor the callback function of each register
    for (uint i = 0; i < mTotalRegNum; i++) {
        mWrCbAPI[mRegArray[i]->my_p->name()][mRegArray[i]->my_p->name()] = NULL;
        mRdCbAPI[mRegArray[i]->my_p->name()][mRegArray[i]->my_p->name()] = NULL;
    }

    //Constructor the mWrCbAPI, mRdCbAPI variable of each register
    mWrCbAPI["PWG0CSDR"]["CSDR"  ] = &Cpwgenc0_regif::cb_PWG0CSDR_CSDR;
    mWrCbAPI["PWG0RDT"]["RDT"   ] = &Cpwgenc0_regif::cb_PWG0RDT_RDT;
    mWrCbAPI["PWG0ST"]["RSF"   ] = &Cpwgenc0_regif::cb_PWG0ST_RSF;
    mWrCbAPI["PWG0CNT"]["CNT"   ] = &Cpwgenc0_regif::cb_PWG0CNT_CNT;
    mWrCbAPI["PWG0TCR"]["TOE"   ] = &Cpwgenc0_regif::cb_PWG0TCR_TOE;
    mWrCbAPI["PWG0TCBR"]["TOBE"  ] = &Cpwgenc0_regif::cb_PWG0TCBR_TOBE;
    mWrCbAPI["PWG0CTL"]["CKS"   ] = &Cpwgenc0_regif::cb_PWG0CTL_CKS;
    mWrCbAPI["PWG0CSBR"]["CSBR"  ] = &Cpwgenc0_regif::cb_PWG0CSBR_CSBR;
    mWrCbAPI["PWG0CRBR"]["CRBR"  ] = &Cpwgenc0_regif::cb_PWG0CRBR_CRBR;
    mWrCbAPI["PWG0CTBR"]["CTBR"  ] = &Cpwgenc0_regif::cb_PWG0CTBR_CTBR;
    mWrCbAPI["PWG0TS"]["TS"    ] = &Cpwgenc0_regif::cb_PWG0TS_TS;
    mWrCbAPI["PWG0TT"]["TT"    ] = &Cpwgenc0_regif::cb_PWG0TT_TT;
    mWrCbAPI["PWG0FOT"]["FOT"   ] = &Cpwgenc0_regif::cb_PWG0FOT_FOT;
    InitLocalVal();
}

/// Destructor: delete pointers
/// @return none
Cpwgenc0_regif::~Cpwgenc0_regif()
{
    // Delete registers' pointer
    for (uint i = 0 ; i < mTotalRegNum ; i++) {
        delete mRegArray[i]->my_p;
        mRegArray[i]->prev = NULL;
    }
    delete [] mRegMap;
    delete [] mRegArray;
    delete mRegList;
    mCurReg = NULL;
}

/// Mask unselected bit
/// @return selected value
Cpwgenc0_regif::uint Cpwgenc0_regif::bit_select(
                            cuint val,    ///< [in] Writting address
                            cuint start,  ///< [in] start bit position
                            cuint end)    ///< [in] end bit position
{
    uint ret_val = val;
    if (end - start + 1 < 32) {
        ret_val = (val>>start) & (uint)((1<<(end-start+1))-1);
    }
    return ret_val;
}

/// Write the value to requested module register
/// @return true if writing transaction is finished normally
bool Cpwgenc0_regif::reg_wr(cuint addr,                    ///< [in] Writting address
                            const unsigned char *p_data,   ///< [in] Writing data
                            cuint size)                    ///< [in] Data size (byte)
{
    if(size == 0) return false;
    #ifdef IS_MODELED_ENDIAN_BIG
    if ((addr % mBusByteWidth) + size > mBusByteWidth) return false;
    #endif

    bool ret_val = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    ret_val = reg_wr_process (addr, p_data, size, false);
    return ret_val;
}

/// Read the register value of requested module
/// @return true if reading transaction is finished normally
bool Cpwgenc0_regif::reg_rd(cuint addr,              ///< [in]  Reading address
                            unsigned char *p_data,   ///< [out] Reading data
                            cuint size)              ///< [in]  Data size (byte)
{
    if(size == 0) return false;
    #ifdef IS_MODELED_ENDIAN_BIG
    if ((addr % mBusByteWidth) + size > mBusByteWidth) return false;
    #endif

    bool ret_val = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    ret_val = reg_rd_process (addr, p_data, size, false);
    return ret_val;
}

/// Write the value to requested module register
/// @return true if writing transaction is finished normally
bool Cpwgenc0_regif::reg_wr_dbg(cuint addr,                    ///< [in] Writting address
                                const unsigned char *p_data,   ///< [in] Writing data
                                cuint size)                    ///< [in] Data size (byte)
{
    if(size == 0) return false;
    #ifdef IS_MODELED_ENDIAN_BIG
    if ((addr % mBusByteWidth) + size > mBusByteWidth) return false;
    #endif

    bool ret_val = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    ret_val = reg_wr_process (addr, p_data, size, true);
    return ret_val;
}

/// Read the register value of requested module
/// @return true if reading transaction is finished normally
bool Cpwgenc0_regif::reg_rd_dbg(cuint addr,              ///< [in]  Reading address
                                unsigned char *p_data,   ///< [out] Reading data
                                cuint size)              ///< [in]  Data size (byte)
{
    if(size == 0) return false;
    #ifdef IS_MODELED_ENDIAN_BIG
    if ((addr % mBusByteWidth) + size > mBusByteWidth) return false;
    #endif

    bool ret_val = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    ret_val = reg_rd_process (addr, p_data, size, true);
    return ret_val;
}

/// Write the value to requested module register
/// @return true if writing transaction is finished normally
bool Cpwgenc0_regif::reg_wr_process(cuint addr,                   ///< [in] Writting address
                                    const unsigned char *p_data,  ///< [in] Writing data
                                    cuint size,                   ///< [in] Data size (byte)
                                    bool IsDbgFunc)               ///< [in] flag indicate reg_rd or reg_rd_dbg
{
    if (mIsReset == true) {
        re_printf("warning", "Cannot write during reset period\n");
        return true;
    }
    uint tmp_addr = addr;
    #ifdef IS_MODELED_ENDIAN_BIG
    if (size < mBusByteWidth) {
        tmp_addr = addr ^ (mBusByteWidth - size);
    }
    #endif
    bool ret_val = false;
    bool valid_addr = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    std::list<uint> list_index;
    uint masked_addr = bit_select(tmp_addr, 0, 6-1);
    uint access_addr = masked_addr;
    while (masked_addr + size > access_addr) {
        int reg_index = get_reg_index(access_addr);
        if (reg_index != -1) {
            vpcl::re_register *reg_p = mRegArray[reg_index]->my_p; 
            if ((IsDbgFunc == true) || (valid_addr == true) || (ChkSize(mRegArray[reg_index]->wacc_size, size, tmp_addr))) {
                valid_addr = true;
                list_index.push_back(reg_index);
            }
            else {
                re_printf("error", "Writing access size to %s at address 0x%08X is wrong: %d byte(s).\n", reg_p->name().c_str(), tmp_addr, size);
                return false;
            }
            access_addr = reg_p->addr() + mRegArray[reg_index]->length;
        } else {
            access_addr += 1;
        }
    }
    if (valid_addr == true) {
        std::list<uint>::iterator it;
        for (it=list_index.begin() ; it!=list_index.end() ; it++) {
            access_addr = masked_addr;
            uint d_size = size;
            uint index = 0;
            if (masked_addr < mRegArray[(*it)]->my_p->addr()) { // adjust access address & index
                access_addr = mRegArray[(*it)]->my_p->addr();
                index = mRegArray[(*it)]->my_p->addr() - masked_addr;
            }
            if ((masked_addr + size) < (mRegArray[(*it)]->my_p->addr() + mRegArray[(*it)]->length)) { // adjust access size
                d_size = masked_addr + size - access_addr;
            } else { // adjust access size
                d_size = (mRegArray[(*it)]->my_p->addr() + mRegArray[(*it)]->length) - access_addr;
            }
            if (size > mRegArray[(*it)]->length) { // adjust index for big endianess
                #ifdef IS_MODELED_ENDIAN_BIG
                index = index ^ (size - mRegArray[(*it)]->length);
                #endif
            }
            ret_val = reg_wr_func(tmp_addr - masked_addr + access_addr, &(p_data[index]), d_size, (*it), IsDbgFunc);
        }
    } else {
        if (IsDbgFunc == false) {
            re_printf("error", "Invalid access address 0x%08X\n", tmp_addr);
        } else {
            ret_val = true;
        }
    }
    return ret_val;
}

/// Read the register value of requested module
/// @return true if reading transaction is finished normally
bool Cpwgenc0_regif::reg_rd_process(cuint addr,              ///< [in]  Reading address
                                    unsigned char *p_data,   ///< [out] Reading data
                                    cuint size,              ///< [in]  Data size (byte)
                                    bool IsDbgFunc)          ///< [in] flag indicate reg_rd or reg_rd_dbg
{
    uint tmp_addr = addr;
    #ifdef IS_MODELED_ENDIAN_BIG
    if (size < mBusByteWidth) {
        tmp_addr = addr ^ (mBusByteWidth - size);
    }
    #endif
    bool ret_val = false;
    bool valid_addr = false;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    std::list<uint> list_index;
    uint masked_addr = bit_select(tmp_addr, 0, 6-1);
    uint access_addr = masked_addr;
    while (masked_addr + size > access_addr) {
        int reg_index = get_reg_index(access_addr);
        if (reg_index != -1) {
            vpcl::re_register *reg_p = mRegArray[reg_index]->my_p; 
            if ((IsDbgFunc == true) || (valid_addr == true) || (ChkSize(mRegArray[reg_index]->racc_size, size, tmp_addr))) {
                valid_addr = true;
                list_index.push_back(reg_index);
            }
            else {
                re_printf("error", "Reading access size to %s at address 0x%08X is wrong: %d byte(s).\n", reg_p->name().c_str(), tmp_addr, size);
                return false;
            }
            access_addr = reg_p->addr() + mRegArray[reg_index]->length;
        } else {
            access_addr += 1;
        }
    }
    if (valid_addr == true) {
        std::list<uint>::iterator it;
        for (it=list_index.begin() ; it!=list_index.end() ; it++) {
            access_addr = masked_addr;
            uint d_size = size;
            uint index = 0;
            if (masked_addr < mRegArray[(*it)]->my_p->addr()) { // adjust access address & index
                access_addr = mRegArray[(*it)]->my_p->addr();
                index = mRegArray[(*it)]->my_p->addr() - masked_addr;
            }
            if ((masked_addr + size) < (mRegArray[(*it)]->my_p->addr() + mRegArray[(*it)]->length)) { // adjust access size
                d_size = masked_addr + size - access_addr;
            } else { // adjust access size
                d_size = (mRegArray[(*it)]->my_p->addr() + mRegArray[(*it)]->length) - access_addr;
            }
            if (size > mRegArray[(*it)]->length) { // adjust index for big endianess
                #ifdef IS_MODELED_ENDIAN_BIG
                index = index ^ (size - mRegArray[(*it)]->length);
                #endif
            }
            ret_val = reg_rd_func(tmp_addr - masked_addr + access_addr, &(p_data[index]), d_size, (*it), IsDbgFunc);
        }
    } else {
        if (IsDbgFunc == false) {
            re_printf("error", "Invalid access address 0x%08X\n", tmp_addr);
        } else {
            memset(p_data, 0, size);
            ret_val = true;
        }
    }
    return ret_val;
}

/// Process reading function
/// @return true if reading successfully
bool Cpwgenc0_regif::reg_rd_func(cuint addr,                     ///< [in]  Reading address
                                 unsigned char *p_data,          ///< [out] Reading data
                                 cuint size,                     ///< [in]  Data size (byte)
                                 cuint reg_index,                ///< [in] Register index
                                 bool IsDbgFunc)                 ///< [in] flag indicate reg_rd or reg_rd_dbg
{
    vpcl::re_register* Register = mRegArray[reg_index]->my_p;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(Register != NULL);
    #else
    sc_assert(Register != NULL);
    #endif
    
    uint pst_data = 0;
    #if 1 // always 1 since any size can be accessed in debug mode 
    uint reg_length = mRegArray[reg_index]->length;
    uint start_pos = 0;
    uint mask_size = (1 << (reg_length/2)) - 1;
    if (size < reg_length) {
        #ifdef IS_MODELED_ENDIAN_BIG
        start_pos =  (reg_length - 1)*8 - (addr & mask_size)*8; // Start bit position
        #else
        start_pos =  (addr & mask_size)*8;  // Start bit position
        #endif
    }
    #endif
    if (IsDbgFunc == false) {
        if (mIsReset == true) {
            re_printf("warning", "Cannot launch call-back function during reset period\n");
        }else {
            #if 1 // always 1 since any size can be accessed in debug mode 
            if (size < reg_length) {
                re_printf("warning","Should read all bit in a register\n");
            }
            #endif
            
            pst_data = Register->read();
            
        }
    }
    pst_data = (uint)(*Register);
    #if 1 // always 1 since any size can be accessed in debug mode 
    if (size < reg_length) {
        uint read_data = 0;
        if (((reg_length%2 == 0) && (reg_length%size == 0) && ((addr&mask_size)%size == 0)) 
         || (IsDbgFunc == false)) {
            for (uint i = start_pos; i < (8*size + start_pos); i++) {
                read_data |= (pst_data & (1 << i)) >> start_pos;
            }
            pst_data = read_data;
        }
    }
    #endif

    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(p_data != NULL);
    #else
    sc_assert(p_data != NULL);
    #endif
    memcpy(p_data, &pst_data, size);
    DumpRegMsg("R", Register->name(), "", size, addr, 0, 0, pst_data, mRegArray[reg_index]->length*8);
    for (vpcl::bit_info *it=Register->mBitInfo ; it!=NULL ; it=it->next) {
        uint val = (uint)(*Register);
        val = bit_select(val, it->mStartAddr, it->mEndAddr);
        DumpRegMsg("R", Register->name(), it->name(), size, addr, 0, 0, val, it->mEndAddr - it->mStartAddr + 1);
    }
    return true;
}

/// Process writing function
/// @return true if writing successfully
bool Cpwgenc0_regif::reg_wr_func(cuint addr,                       ///< [in] Writting address
                                 const unsigned char *p_data,      ///< [in] Writing data
                                 cuint size,                       ///< [in] Data size (byte)
                                 cuint reg_index,                  ///< [in] Register index
                                 bool IsDbgFunc)                   ///< [in] flag indicate reg_wr or reg_wr_dbg
{
    vpcl::re_register* Register = mRegArray[reg_index]->my_p;
    uint pre_data = 0;
    uint data = 0;
    #if 1 // always 1 since any size can be accessed in debug mode 
    uint reg_length = mRegArray[reg_index]->length;
    uint start_pos = 0;
    #endif
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert ((p_data != NULL) && (Register != NULL));
    #else
    sc_assert ((p_data != NULL) && (Register != NULL));
    #endif
    memcpy(&data, p_data, size);
    #if 1 // always 1 since any size can be accessed in debug mode 
    if (size < reg_length) {
        uint mask_size = (1 << (reg_length/2)) - 1;
        if (((reg_length%2 == 0) && (reg_length%size == 0) && ((addr&mask_size)%size == 0)) 
         || (IsDbgFunc == false)) {
            uint new_data = (uint)(*Register);  // For writing to register
            uint written_data = 0;         // For transferring to callback function
            #ifdef IS_MODELED_ENDIAN_BIG
            start_pos =  (reg_length - 1)*8 - (addr & mask_size)*8; // Start bit position
            #else
            start_pos =  (addr & mask_size)*8;  // Start bit position
            #endif
            for (uint i = start_pos; i < (8*size + start_pos); i++) {
                new_data = (new_data & (~(1 << i))) | ((data << start_pos) & (1 << i));
                written_data = (written_data & (~(1 << i))) | ((data << start_pos) & (1 << i));
            }
            data = new_data;
        }
    }
    #endif

    if ((mRegArray[reg_index]->block == false) || (IsDbgFunc == true)){
        pre_data = (uint)(*Register);
        if (IsDbgFunc == true) {
            bool backup_warning = Register->mMessageLevel["warning"];
            bool backup_error = Register->mMessageLevel["error"];
            Register->mMessageLevel["warning"]   = false;
            Register->mMessageLevel["error"]   = false;

            Register->write(data);

            Register->mMessageLevel["warning"]   = backup_warning;
            Register->mMessageLevel["error"]   = backup_error;
        } else {
            Register->write(data);
        }
        UpdateLocalVal(Register->addr());
        DumpRegMsg("W", Register->name(), "", size, addr, data, pre_data, (uint)(*Register), mRegArray[reg_index]->length*8);
        for (vpcl::bit_info *it=Register->mBitInfo ; it!=NULL ; it=it->next) {
            uint val = (uint)(*Register);
            val = bit_select(val, it->mStartAddr, it->mEndAddr);
            uint pre_val = pre_data;
            pre_val = bit_select(pre_val, it->mStartAddr, it->mEndAddr);
            DumpRegMsg("W", Register->name(), it->name(), size, addr, 0, pre_val, val, it->mEndAddr - it->mStartAddr + 1);
            if ((mWrCbAPI[Register->name()][it->name()]) != NULL) {
                (this->*(mWrCbAPI[Register->name()][it->name()]))(RegCBstr(mRegArray[reg_index]->channel, true, size, pre_data, data));
            }
        }
    }
    else {
        re_printf("warning","%s is blocked writing from Bus I/F.\n", Register->name().c_str());
    }
    return true;
}

/// Check access size
/// @@return true if detect_size equal expect_size
bool Cpwgenc0_regif::ChkSize(std::string expect_size, cuint detect_size, cuint addr)
{
    std::vector<std::string> arg_vec = Str2Vec(expect_size, '|');
    std::vector<std::string>::iterator it;
    char *err_p = NULL;
    uint size = 0;
    if (detect_size > mBusByteWidth) {
        re_printf("error", "Invalid access size: %d bytes\n", detect_size);
        return false;
    }
    if(addr%detect_size != 0) {
        re_printf("error", "Invalid access address 0x%08X with access size %d bytes\n", addr, detect_size);
        return false;
    }
    for (it = arg_vec.begin(); it != arg_vec.end(); it++) {
        errno = 0; size = (uint) strtoul((*it).c_str(), &err_p, 0);
        #ifdef REGIF_NOT_USE_SYSTEMC
        assert(err_p != NULL);
        #else
        sc_assert(err_p != NULL);
        #endif
        if (errno != ERANGE && *err_p == '\0') {
            if ((size/8) == detect_size) {
                return true;
            }
        }
    }
    return false;
}

/// Initialize reg_handle_command variables
/// @return none
void Cpwgenc0_regif::CommandInit()
{
    mDumpRegisterRW = false;
    mMessageLevel["fatal"]   = true;
    mMessageLevel["error"]   = true;
    mMessageLevel["warning"] = false;
    mMessageLevel["info"]    = false;
}

/// Process reg_handle_command command
/// @return string
std::string Cpwgenc0_regif::reg_handle_command(const std::vector<std::string>& args)
{
    std::string ret = "";
    std::string err_msg = "Error ("   + mInstName + ") ";   // error message
    if (args[0] == "reg") {
        if (args[1] == "help") {
            ret += "    --- reg ---\n";
            ret += "    reg MessageLevel <fatal|error|warning|info> Select debug message level (Default: fatal,error)\n";
            ret += "    reg DumpRegisterRW <true/false>             Select dump register access information  (Default: false)\n";
            ret += "    reg <register_name> MessageLevel <fatal|error|warning|info> Select debug message level for register (Default: fatal,error)\n";
            ret += "    reg <register_name> force <value>           Force register with setting value\n";
            ret += "    reg <register_name> release                 Release register from force value\n";
            ret += "    reg <register_name> <value>                 Write a value into register\n";
            ret += "    reg <register_name>                         Read value of register\n";
            ret += "    reg help                                    Show a direction\n";
        }
        else if (args[1] == "DumpRegisterRW") {
            if (args.size() == 2) {
                ret = (mDumpRegisterRW)? "true" : "false";
            }
            else if (args.size() == 3) {
                if (args[2] == "true") {
                    mDumpRegisterRW = true;
                }
                else if (args[2] == "false") {
                    mDumpRegisterRW = false;
                } else {
                    ret = err_msg + "DumpRegisterRW command needs an argument [true/false]\n";
                }
            } else {
                ret = err_msg + "DumpRegisterRW has too much arguments\n";
            }
        }
        else if (args[1] == "MessageLevel") {
            // read mode
            if (args.size() == 2) {
                std::map<std::string, bool>::iterator it;
                for (it = mMessageLevel.begin(); it != mMessageLevel.end(); it++) {
                    if ( it->second == true ) {
                        ret += " " + it->first;
                    }
                }
            // write mode
            } else if (args.size() == 3) {
                std::vector<std::string> arg_vec = Str2Vec(args[2], '|');
                std::map<std::string, bool>::iterator msgit;
                for (msgit =mMessageLevel.begin(); msgit != mMessageLevel.end(); msgit++) {
                    msgit->second = false;
                }
                std::vector<std::string>::iterator it;
                for (it = arg_vec.begin(); it != arg_vec.end(); it++) {
                    mMessageLevel[*it] = true;
                }
            } else {
                ret = err_msg + "MessageLevel has too much arguments\n";
            }
        } else {
            bool matched = false;
            for (uint i = 0; i < mTotalRegNum; i++) {
                if (args[1] == mRegArray[i]->my_p->name()) {
                    ret = AccessRegCommand(args, mRegArray[i]->my_p, mRegArray[i]->block);
                    matched = true;
                    break;
                }
            }
            if (!matched) {
                ret = err_msg + "Register name " + (std::string)args[1] + " is invalid\n";
            }
        }
    }
    return ret;
}

/// process reg_handle_command of "reg" parameter
/// @return string
std::string Cpwgenc0_regif::AccessRegCommand(const std::vector<std::string>& args, vpcl::re_register *Register, bool &BlockReg)
{
    std::string ret = "";
    char *err_p = NULL;
    std::string err_msg = "Error ("   + mInstName + ") ";   // error message
    uint data = 0;
    std::ostringstream str_tmp;
    #ifdef REGIF_NOT_USE_SYSTEMC
    assert(Register != NULL);
    #else
    sc_assert(Register != NULL);
    #endif
    if (args.size() == 2) {
        data = (uint)(*Register);
        str_tmp<<std::setw(14)<<std::left<<(Register->name());
        str_tmp<<"0x"<<std::setw(8)<<std::hex<<std::uppercase<<std::left<<data<<"\n";
        ret = str_tmp.str();
    } else if ((args.size() == 3) && (args[2] == "release")) {
        BlockReg = false;
    } else if ((args.size() == 4) && (args[2] == "force")) {
        errno = 0; data = (uint) strtoul(args[3].c_str(), &err_p, 0);
        #ifdef REGIF_NOT_USE_SYSTEMC
        assert(err_p != NULL);
        #else
        sc_assert(err_p != NULL);
        #endif
        if (errno != ERANGE && *err_p == '\0') {
            *Register = data;
            BlockReg = true;
            UpdateLocalVal(Register->addr());
        } else {
            ret = err_msg + "[" + Register->name() + "] Invalid force value\n";
        }
    } else if ((args.size() >= 3) && (args[2] == "MessageLevel")) {
        if (args.size() == 3) {
            ret = Register->name() + " ";
            ret += Register->GetMessageLevel();
        } else if (args.size() == 4) {
            std::map<std::string, bool> MessageLevel;
            std::vector<std::string> arg_vec = Str2Vec(args[3], '|');
            std::vector<std::string>::iterator it;
            for (it = arg_vec.begin(); it != arg_vec.end(); it++) {
                MessageLevel[*it] = true;
            }
            Register->SetMessageLevel(MessageLevel);
        } else {
            ret = err_msg + "[" + Register->name() + "] MessageLevel has too much arguments\n";
        }
    } else if ((args.size() == 3) && (args[2] != "release")) {
        errno = 0; data = (uint) strtoul(args[2].c_str(), &err_p, 0);
        if (errno != ERANGE && *err_p == '\0') {
            *Register = data;
            UpdateLocalVal(Register->addr());
        } else {
            ret = err_msg + "[" + Register->name() + "] Invalid write value\n";
        }
    } else {
        ret = err_msg + "Wrong command : ";
        for (uint index = 0; index < args.size(); index ++) {
            ret += args[index] + " ";
        }
    }
    return ret;
}

/// Initialize local variables
/// @return none
void Cpwgenc0_regif::InitLocalVal()
{
    // Initialize local variables
    PWG0CSDR_CSDR = 0x0;
    PWG0CRDR_CRDR = 0x0;
    PWG0CTDR_CTDR = 0x0;
    PWG0RDT_RDTT = 0x0;
    PWG0RDT_RDT = 0x0;
    PWG0ST_FOF = 0x0;
    PWG0ST_RSFT = 0x0;
    PWG0ST_RSF = 0x0;
    PWG0CNT_CNT = 0xFFFF;
    PWG0TCR_TOE = 0x1;
    PWG0TCBR_TOBE = 0x1;
    PWG0CTL_TFIE = 0x1;
    PWG0CTL_TRIE = 0x0;
    PWG0CTL_FOS = 0x0;
    PWG0CTL_OCL = 0x0;
    PWG0CTL_TCUT = 0x0;
    PWG0CTL_CKS = 0x0;
    PWG0CSBR_CSBR = 0x0;
    PWG0CRBR_CRBR = 0x0;
    PWG0CTBR_CTBR = 0x0;
    PWG0TE_TE = 0x0;
    PWG0TS_TS = 0x0;
    PWG0TT_TT = 0x0;
    PWG0FOT_FOT = 0x0;
}

/// Update bit value to local value
/// @return None
void Cpwgenc0_regif::UpdateLocalVal(cuint addr)
{
    if (addr == 0x0000) {
        PWG0CSDR_CSDR                = (*PWG0CSDR)["CSDR"    ];
        return;
    }
    if (addr == 0x0004) {
        PWG0CRDR_CRDR                = (*PWG0CRDR)["CRDR"    ];
        return;
    }
    if (addr == 0x0008) {
        PWG0CTDR_CTDR                = (*PWG0CTDR)["CTDR"    ];
        return;
    }
    if (addr == 0x000C) {
        PWG0RDT_RDTT                 = (*PWG0RDT )["RDTT"    ];
        PWG0RDT_RDT                  = (*PWG0RDT )["RDT"     ];
        return;
    }
    if (addr == 0x0010) {
        PWG0ST_FOF                   = (*PWG0ST  )["FOF"     ];
        PWG0ST_RSFT                  = (*PWG0ST  )["RSFT"    ];
        PWG0ST_RSF                   = (*PWG0ST  )["RSF"     ];
        return;
    }
    if (addr == 0x0014) {
        PWG0CNT_CNT                  = (*PWG0CNT )["CNT"     ];
        return;
    }
    if (addr == 0x0018) {
        PWG0TCR_TOE                  = (*PWG0TCR )["TOE"     ];
        return;
    }
    if (addr == 0x001C) {
        PWG0TCBR_TOBE                = (*PWG0TCBR)["TOBE"    ];
        return;
    }
    if (addr == 0x0020) {
        PWG0CTL_TFIE                 = (*PWG0CTL )["TFIE"    ];
        PWG0CTL_TRIE                 = (*PWG0CTL )["TRIE"    ];
        PWG0CTL_FOS                  = (*PWG0CTL )["FOS"     ];
        PWG0CTL_OCL                  = (*PWG0CTL )["OCL"     ];
        PWG0CTL_TCUT                 = (*PWG0CTL )["TCUT"    ];
        PWG0CTL_CKS                  = (*PWG0CTL )["CKS"     ];
        return;
    }
    if (addr == 0x0024) {
        PWG0CSBR_CSBR                = (*PWG0CSBR)["CSBR"    ];
        return;
    }
    if (addr == 0x0028) {
        PWG0CRBR_CRBR                = (*PWG0CRBR)["CRBR"    ];
        return;
    }
    if (addr == 0x002C) {
        PWG0CTBR_CTBR                = (*PWG0CTBR)["CTBR"    ];
        return;
    }
    if (addr == 0x0030) {
        PWG0TE_TE                    = (*PWG0TE  )["TE"      ];
        return;
    }
    if (addr == 0x0034) {
        PWG0TS_TS                    = (*PWG0TS  )["TS"      ];
        return;
    }
    if (addr == 0x0038) {
        PWG0TT_TT                    = (*PWG0TT  )["TT"      ];
        return;
    }
    if (addr == 0x003C) {
        PWG0FOT_FOT                  = (*PWG0FOT )["FOT"     ];
        return;
    }
}

/// Update local value to bit value
/// @return None
void Cpwgenc0_regif::UpdateRegVal(cuint addr)
{
    if (addr == 0x0000) {
        (*PWG0CSDR)["CSDR"    ] = PWG0CSDR_CSDR;
        return;
    }
    if (addr == 0x0004) {
        (*PWG0CRDR)["CRDR"    ] = PWG0CRDR_CRDR;
        return;
    }
    if (addr == 0x0008) {
        (*PWG0CTDR)["CTDR"    ] = PWG0CTDR_CTDR;
        return;
    }
    if (addr == 0x000C) {
        (*PWG0RDT )["RDTT"    ] = PWG0RDT_RDTT;
        (*PWG0RDT )["RDT"     ] = PWG0RDT_RDT;
        return;
    }
    if (addr == 0x0010) {
        (*PWG0ST  )["FOF"     ] = PWG0ST_FOF;
        (*PWG0ST  )["RSFT"    ] = PWG0ST_RSFT;
        (*PWG0ST  )["RSF"     ] = PWG0ST_RSF;
        return;
    }
    if (addr == 0x0014) {
        (*PWG0CNT )["CNT"     ] = PWG0CNT_CNT;
        return;
    }
    if (addr == 0x0018) {
        (*PWG0TCR )["TOE"     ] = PWG0TCR_TOE;
        return;
    }
    if (addr == 0x001C) {
        (*PWG0TCBR)["TOBE"    ] = PWG0TCBR_TOBE;
        return;
    }
    if (addr == 0x0020) {
        (*PWG0CTL )["TFIE"    ] = PWG0CTL_TFIE;
        (*PWG0CTL )["TRIE"    ] = PWG0CTL_TRIE;
        (*PWG0CTL )["FOS"     ] = PWG0CTL_FOS;
        (*PWG0CTL )["OCL"     ] = PWG0CTL_OCL;
        (*PWG0CTL )["TCUT"    ] = PWG0CTL_TCUT;
        (*PWG0CTL )["CKS"     ] = PWG0CTL_CKS;
        return;
    }
    if (addr == 0x0024) {
        (*PWG0CSBR)["CSBR"    ] = PWG0CSBR_CSBR;
        return;
    }
    if (addr == 0x0028) {
        (*PWG0CRBR)["CRBR"    ] = PWG0CRBR_CRBR;
        return;
    }
    if (addr == 0x002C) {
        (*PWG0CTBR)["CTBR"    ] = PWG0CTBR_CTBR;
        return;
    }
    if (addr == 0x0030) {
        (*PWG0TE  )["TE"      ] = PWG0TE_TE;
        return;
    }
    if (addr == 0x0034) {
        (*PWG0TS  )["TS"      ] = PWG0TS_TS;
        return;
    }
    if (addr == 0x0038) {
        (*PWG0TT  )["TT"      ] = PWG0TT_TT;
        return;
    }
    if (addr == 0x003C) {
        (*PWG0FOT )["FOT"     ] = PWG0FOT_FOT;
        return;
    }
}

/// convert string to vector
/// @return vector
std::vector<std::string> Cpwgenc0_regif::Str2Vec(std::string str, const char sep)
{
    std::vector<std::string> buf;
    std::string::size_type index = 0;
    for (uint i=0 ; i<str.size() ; i++) {
        if (str[i] == sep) {
            buf.push_back(str.substr(index, i-index));
            index = i+1;
        }
    }
    buf.push_back(str.substr(index));
    return buf;
}

/// Align nuber to hexadecimal format
/// @return aligned number
std::string Cpwgenc0_regif::Num2HexStr(cuint num, cuint num_length, bool space_strip)
{
    std::ostringstream str_tmp;
    #ifdef MSG_WO_WR_DATA
    str_tmp<<"0x"<<std::hex<<std::uppercase<<std::right<<num;
    str_tmp<<std::setfill(' ')<<std::setw(10-str_tmp.str().length())<<"";
    #else
    uint total_length = (num_length + 3)/4; // Calculate number of hexadecimal number
    str_tmp<<"0x"<<std::setfill('0')<<std::setw(total_length)<<std::hex<<std::uppercase<<std::right<<num;
    if (!space_strip) {
        str_tmp<<std::setfill(' ')<<std::setw(10-str_tmp.str().length())<<"";   
    }
    #endif
    return str_tmp.str();
}

/// Dump register access information
/// @return none
void Cpwgenc0_regif::DumpRegMsg(const std::string operation, const std::string RegName, const std::string BitName, cuint size, cuint addr, cuint wr_data, cuint pre_data, cuint data, cuint reg_length)
{
    if(mDumpRegisterRW == true) {
        std::string reg_name = RegName;
        std::ostringstream str_tmp;
        if (BitName != "") {
            reg_name += ":" + BitName;
        }
        str_tmp<<"REG ["<<std::setw(15)<<std::left<<reg_name<<"] ";
        str_tmp<<operation;
        str_tmp<<" Size= "<<size;
        str_tmp<<" Addr= "<<Num2HexStr(addr,6, false);
        if (operation == "R") {
            str_tmp<<" Data= "<<Num2HexStr(data    , reg_length, true);
        } else {
            #ifdef MSG_WO_WR_DATA
            str_tmp<<" Data= "<<Num2HexStr(pre_data, reg_length, false);
            #else
            str_tmp<<" Data= "<<Num2HexStr(wr_data , reg_length, false);
            str_tmp<<" : "    <<Num2HexStr(pre_data, reg_length, false);
            #endif
            str_tmp<<" => "   <<Num2HexStr(data    , reg_length, true);
        }
        re_printf("DumpRegMsg", "%s\n", str_tmp.str().c_str());
    }
}

/// find register pointer based on accessed address
/// @return accessed register pointer
int Cpwgenc0_regif::get_reg_index(cuint access_addr)
{
    if (mRegMap[access_addr] != (1<<6)) {
        return mRegMap[access_addr];
    } else {
        return -1;
    }
}

/// find first register pointer
/// @return first register pointer
vpcl::re_register *Cpwgenc0_regif::first_reg_object()
{
    mCurReg = mRegList;
    return mCurReg->my_p;
}

/// find next register pointer
/// @return next register pointer
vpcl::re_register *Cpwgenc0_regif::next_reg_object()
{
    if (mCurReg != NULL) {
        mCurReg = mCurReg->prev;
        if (mCurReg != NULL) {
            return mCurReg->my_p;
        }
    }
    return NULL;
}

/// write callback function of CoWare
/// @return none
void Cpwgenc0_regif::wr_cb(cuint addr, uint data)
{
    #ifdef USR_CWR_SYSTEMC
    cwmem.put(data, addr&0xFFFF);
    #else
    array[addr&0xFFFF] = data;
    #endif
}

/// read callback function of CoWare
/// @return read data
Cpwgenc0_regif::uint Cpwgenc0_regif::rd_cb(cuint addr)
{
    #ifdef USR_CWR_SYSTEMC
    return cwmem.get(addr&0xFFFF);
    #else
    return array[addr&0xFFFF];
    #endif
}

/// print message function
/// @return none
void Cpwgenc0_regif::_re_printf(std::string group, const char *message, ...)
{
    // message group check
    if (group == "DumpRegMsg") {
        group = "info";
    } else {
        #ifndef REGIF_SC_REPORT
        if (mMessageLevel[group] == false) return;
        #endif//REGIF_SC_REPORT
    }

    if (message == NULL) return;

    // print header
    #ifndef REGIF_NOT_USE_SYSTEMC
    std::stringstream t_stream;
    std::string cur_time;

    double tu_value = 0;
    std::string tu_name = "";
    if (sc_get_time_resolution() < sc_time(1, SC_PS)) {
        tu_value = 1000000000000000LL;
        tu_name = " fs";
    }
    else if (sc_get_time_resolution() < sc_time(1, SC_NS)) {
        tu_value = 1000000000000LL;
        tu_name = " ps";
    }
    else if (sc_get_time_resolution() < sc_time(1, SC_US)) {
        tu_value = 1000000000;
        tu_name = " ns";
    }
    else if (sc_get_time_resolution() < sc_time(1, SC_MS)) {
        tu_value = 1000000;
        tu_name = " us";
    }
    else if (sc_get_time_resolution() < sc_time(1, SC_SEC)) {
        tu_value = 1000;
        tu_name = " ms";
    }
    else {
        tu_value = 1;
        tu_name = " s";
    }
    t_stream << sc_time_stamp().value() * (uint64) (sc_get_time_resolution().to_seconds() * tu_value) << tu_name;

    cur_time = t_stream.str();
    #else
    std::string cur_time = "";
    #endif

    #ifndef REGIF_SC_REPORT
    #ifdef MSG_COLOR
    // color setting
    //
    // severity color       color Linux       Windows
    // fatal    RED         RED   \033[0;31m  FOREGROUND_RED
    // error    RED         BLUE  \033[0;34m  FOREGROUND_BLUE
    // warning  RED
    // info     BLUE
    #ifdef _WIN32
    HANDLE hStdout;
    CONSOLE_SCREEN_BUFFER_INFO csbi;
    if (group == "fatal" || group == "error" || group == "warning") {
        SetConsoleTextAttribute(hStdout, FOREGROUND_RED);
    }
    else if (group == "info" ){
        SetConsoleTextAttribute(hStdout, FOREGROUND_BLUE);
    }
    #else
    if (group == "fatal" || group == "error" || group == "warning") {
        printf("\033[0;31m");
    }
    else if (group == "info" ){
        printf("\033[0;34m");
    }
    #endif
    #endif //MSG_COLOR
    printf("%8s [%20s] (%10s) ", group.c_str(), cur_time.c_str(), mInstName.c_str());
    #endif//REGIF_SC_REPORT

    // print body
    va_list argptr;
    va_start(argptr, message);
    if (argptr == NULL) return;
    #ifdef REGIF_SC_REPORT
    char str[1024];
    sprintf(str, "[%20s] ", cur_time.c_str());
    vsprintf(str+23, message, argptr);
    if (group == "fatal") {
        SC_REPORT_FATAL(mInstName.c_str(), str);
    }
    else if (group == "error") {
        SC_REPORT_ERROR(mInstName.c_str(), str);
    }
    else if (group == "warning") {
        SC_REPORT_WARNING(mInstName.c_str(), str);
    }
    else if (group == "info") {
        SC_REPORT_INFO(mInstName.c_str(), str);
    }
    #else //REGIF_SC_REPORT
    vprintf(message, argptr);
    #endif//REGIF_SC_REPORT

    #ifndef REGIF_SC_REPORT
    #ifdef DumpFileNameLineNum
    // print footer
    if (group == "fatal" || group == "error") {
        printf(" [%s:%d]\n", mFileName.c_str(), mLineNum);
    }
    #endif//DumpFileNameLineNum
    #endif//REGIF_SC_REPORT
    va_end(argptr);

    #ifndef REGIF_SC_REPORT
    #ifdef MSG_COLOR
    #ifdef _WIN32
    SetConsoleTextAttribute(hStdout, csbi.wAttributes);
    #else
    printf("\033[m");
    #endif
    #endif //MSG_COLOR
    #endif//REGIF_SC_REPORT

    fflush(stdout);
    fflush(stderr);

    if (group == "fatal") {
        exit(1);
    }
}

/// Get file name and line number
/// @return none
void Cpwgenc0_regif::get_fileline(std::string filename, int line_number)
{
    mFileName = filename;
    mLineNum  = line_number;
}

/// API for instance registration
/// @return none
void Cpwgenc0_regif::set_instance_name(std::string InstName)
{
    mInstName = InstName;
}

/// Method to change value of mIsReset
/// @return none
void Cpwgenc0_regif::EnableReset(const bool is_active)
{
    // update register IF class's reset status
    mIsReset = is_active;

    // update re_register's reset status
    for (uint i = 0; i < mTotalRegNum; i++) {
        mRegArray[i]->my_p->EnableReset(is_active);
        if (is_active) {
            re_printf("info", "Initialize %s (%08x)\n", mRegArray[i]->my_p->name().c_str(), (uint)(*mRegArray[i]->my_p));
        }
    }

    if(is_active) {
        InitLocalVal(); // Initialize local variable
    }
}

#ifdef USR_CWR_SYSTEMC
uint Cpwgenc0_regif::cw_rd_cb(tlm::tlm_generic_payload& trans, int tag)
{
    unsigned char* ptr = (unsigned char*)trans.get_data_ptr();
    memset(ptr,0,sizeof(uint));
    reg_rd_dbg((cuint)tag, ptr, sizeof(uint));
    return sizeof(uint);
}

uint Cpwgenc0_regif::cw_wr_cb(tlm::tlm_generic_payload& trans, int tag)
{
    unsigned char* ptr = (unsigned char*)trans.get_data_ptr();
    reg_wr_dbg((cuint)tag, ptr, sizeof(uint));
    return sizeof(uint);
}
void Cpwgenc0_regif::cw_instantiate_reg(scml2::reg<uint> * reg, std::string reg_name, int offset)
{
    reg = new scml2::reg<uint>(reg_name, cwmem, offset/sizeof(uint));
    scml2::set_debug_write_callback(*reg,SCML2_CALLBACK(cw_wr_cb),offset);
    scml2::set_debug_read_callback(*reg,SCML2_CALLBACK(cw_rd_cb),offset);
}
#endif
